# Forge monorepo workspace. Layout flattened to root per ADR-001.
packages:
  - "packages/*"
  - "apps/*"
  - "examples/*"
  # Resilience harness: real processes, real containers, no coverage floor.
  - "harness"
